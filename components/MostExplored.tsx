"use client";
import { useEffect, useState } from "react";
type Row={character_id:string;character_name:string;views:number};
export default function MostExplored(){const [items,setItems]=useState<Row[]>([]);const [configured,setConfigured]=useState<boolean|null>(null);useEffect(()=>{fetch('/api/views').then(r=>r.json()).then(d=>{setItems(d.items||[]);setConfigured(Boolean(d.configured))}).catch(()=>setConfigured(false))},[]);return <section className="section"><div className="container"><div className="section-head"><div><div className="eyebrow">Live discovery</div><h2>Most explored wizards</h2></div></div>{items.length?<div className="grid-3">{items.map((x,i)=><div className="card card-pad" key={x.character_id}><div className="tag">#{i+1}</div><h3>{x.character_name}</h3><p className="muted">{x.views} profile views</p></div>)}</div>:<div className="card card-pad"><h3>{configured?"No views yet":"Realtime leaderboard ready"}</h3><p className="muted">{configured?"Open a character profile to generate the first live statistic.":"Connect Supabase using the documented server-side environment variables to activate the live leaderboard. The rest of the app remains fully functional without it."}</p></div>}</div></section>}
